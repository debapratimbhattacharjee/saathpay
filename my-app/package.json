{
  "name": "my-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start:web": "npm start --workspace web",
    "start:native": "npm start --workspace native",
    "build:web": "npm run build --workspace web",
    "test:web": "npm test --workspace web"
  }
}
